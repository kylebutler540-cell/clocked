# Memory — 2026-03-23

## Cost Optimization Changes Made Today
- Heartbeats: changed from every 2hrs to every 4hrs (8am, 12pm, 4pm, 8pm)
- Weather-bot cron: switched to google/gemini-flash-2.0
- Morning-briefing cron: switched to google/gemini-2.5-flash-preview
- **Coding agents switched BACK to Claude Sonnet** (anthropic/claude-sonnet-4-6) after Gemini produced poor results on Clocked app tasks
- MEMORY.md Clocked design section condensed to save tokens per message
- Lukas approved all these changes

## Clocked App — Work Done Today
- **Scrollbar fix (desktop):** Moved scrollbar from next to posts to far right viewport edge. Required multiple CSS iterations — final fix used `align-self: start` on `.app-main`, `overflow: visible !important` on `.page-content` on desktop, and `overflow-y: auto` on body at 768px+. Also hid inner scrollbars via `scrollbar-width: none`.
- **Location pill (desktop topbar):** Moved from left section (next to logo) to center section (left of search bar). Updated `.desktop-topbar-search` to use flexbox with gap.
- **Comment edit/delete:** Backend routes (PUT/DELETE /api/posts/:postId/comments/:id) built. Frontend 3-dot menu in PostDetail.jsx for web, long-press for mobile. Fixed menuRef bug (was single ref, now map via menuRefs.current[id]).
- **Post edit/delete:** Backend routes (PUT/DELETE /api/posts/:id) built. PostCard.jsx has full 3-dot menu, inline edit, delete with confirm.
- **Account merge on Google login:** When user signs in with Google, anonymous posts/comments get reassigned to Google user ID so ownership checks work.
- **Mobile sidebar:** Added Saved link + location button to SideDrawer.jsx. Removed location pill from Home.jsx mobile.
- **Onboarding:** Location prompt added if no location set.

## Lukas Preferences Learned Today
- Wants a proactive Telegram ping (via message tool) when tasks complete — not just a reply in the thread
- Always screenshot the browser to verify changes before sending the link
- Keep replies short — no walls of explanation, just fix it and confirm
- Every time something is deployed, open it in browser AND send the link in Telegram

## Git State
- Repo: /Users/kylebutler/.openclaw/workspace/clocked/app
- Remote: https://github.com/kylebutler540-cell/clocked (user: kylebutler540-cell)
- Latest commit: location pill moved to left of search bar
- All changes pushed and live on Railway

## Coding Agent Rule
- Default: Claude Sonnet for all coding tasks (Gemini was tried and failed today)
- Always batch related tasks into one agent run
- Use Claude Code with --permission-mode bypassPermissions --print (no PTY)
- Write task prompts to /tmp/ files to avoid shell quoting issues with long prompts
