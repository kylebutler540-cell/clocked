# Memory — 2026-04-04

## Clocked App — Major Work Session

### Cursor Setup
- Introduced Lukas to Cursor (AI code editor) as a faster alternative to telling me changes
- Cursor installed at /Applications/Cursor.app, Clocked project opened at /Users/kylebutler/.openclaw/workspace/clocked
- Cmd+L opens chat sidebar, Cmd+K for inline edits

### Company Logos — Overhaul
- Clearbit logo API is DEAD (acquired by HubSpot, no longer reliable)
- Switched to Google Favicon service: `https://www.google.com/s2/favicons?sz=64&domain={domain}`
- Built `brandLogo.js` — brand normalizer with 60+ hardcoded brand→domain mappings
  - Handles variants: "Amazon Distribution Center" → amazon.com, "Culver's" → culvers.com, etc.
  - Fallback chain: brand map → Google Places website → name heuristic
  - Caches in `employer_logo` DB table, 30-day TTL
- Old Clearbit URLs cleared from DB cache so everything re-fetches with Google favicons

### Railway / DB Issues Encountered & Fixed
- Multiple failed migrations in DB (`20260328000000_add_company_ratings`) were blocking startup
- Fixed by switching startCommand to use `prisma db push` instead of `migrate deploy`
- railway.toml start command: `cd backend && npx prisma db push --schema=../prisma/schema.prisma --accept-data-loss && npm install && node src/index.js`
- Frontend deploy ALWAYS requires: `cd /Users/kylebutler/.openclaw/workspace/clocked/app/frontend && railway up --service frontend`
- Backend auto-deploys via git push to main

### Search System — Overhaul
- Switched from Places API v1 Autocomplete → Text Search (returns coords, all nearby matches)
- Added Haversine distance sorting — strictly nearest→farthest
- Added department filtering (no Costco Pharmacy, Walmart Vision Center etc. in general searches)
- Added deduplication by physical location (300m grid)
- Short query expansion: "cos" → also searches "Costco" in parallel, merges + re-ranks
- Relevance scoring: prefix match + brand recognition + name length penalty (shorter = more likely main brand)
- Distance format: anything < 1 mile → "< 1 mile" (no feet display)
- ZIP code input supported — geocodes to lat/lng center point

### Data Cleanup
- "Test Company" posts remapped to real Walmart Supercenter (place_id: ChIJgUYJpuBNGIgR8PGiUfuZkEs)
- Correct address: 5859 28th St SE, Grand Rapids, MI 49546, USA
- "Test Co" / "test123" fake posts deleted from DB
- Logo cache cleared for fake IDs

### Alerts / Notifications — Major Overhaul
- Backend enrichment: always re-fetches actor data fresh; fallback chain: display_name → username → User #N
- Self-alerts: filtered at both creation time and fetch time
- Comment preview: notification text includes actual comment body inline
- `comment_body` backfilled from DB for older notifications missing it
- Comment like from Alerts tab: persists to DB (was only visual before)
- Like state preloaded in nav state when navigating from Alerts → Post (no delayed flip)
- Like count also preloaded (not just liked boolean) — instant count + icon on open
- ActionIcon badges removed from notification cards
- Like button: only heart turns red, rest of button stays gray

### PostDetail — Portal Fix
- PostDetail now renders via `createPortal` into document.body at z-index 900
- Solid black background — no layout bleed-through
- Post card behind CommentSheet removed entirely (user didn't want anything in background)
- CommentSheet overlay made opaque when in fullscreen/portal mode

### Reply Edit Support
- Reply editing added — same UI as top-level comment edit
- Edit props flow down to nested replies via `{...editProps}`
- Backend already supported reply edit (replies are comments with parent_id)

### Comment Highlight on Open
- PostDetail reads `location.state.highlightComment`
- Passes `highlightCommentId` to CommentSheet → CommentItem
- Matching comment scrolls into view after 400ms, purple flash for 2.5s

### Empty State Fix
- Company page with active filter (Bad/Neutral/Good) + no results → shows "Write a review" instead of "Be the first to review"
- No filter active → still shows "Be the first to review this workplace"

## Trading Bot Status (as of morning briefing)
- Balance: ~$89.99 total ($17.05 USDC cash + ETH position)
- Down ~$10.50 from starting $100.49
- Holding ETH-USDC position, no overnight trades
- v3 strategy: 1hr candles, RSI <28 buy, RSI >68 or +3.5% TP or -3% SL
