# Memory — April 13, 2026

## Clocked App — Changes This Session (April 12–13)

### Report System
- FlagModal.jsx fully rebuilt — no mailto, submits directly to `/posts/:id/flag` backend API
- To/Subject email header fields removed entirely (both mobile and desktop)
- "Other" reason auto-focuses Additional Details textarea
- Success state: green checkmark inside modal
- Report notifications → Clocked Reports Telegram group (new group, not main chat)
- New bot: @Clockedreportsbot — token stored in Railway as TELEGRAM_BOT_TOKEN
- Group chat ID: -5000785765
- Telegram notifications have two inline buttons: 👁 View Post + 🗑 Delete Post
- Delete uses GET `/api/posts/:id/admin-delete?secret=ADMIN_DELETE_SECRET` route
- telegramNotify.js: escape function only escapes Markdown chars, not URLs; reason extracted from full body

### Profile Flow
- ProfileSetup.jsx split into edit mode (`?mode=edit`) and new account mode
- Edit mode: pre-fills fields from user context, shows back button, no nav lock, 2 steps, returns to /profile
- New account mode: blank fields, locked navigation, no back button, 3 steps
- Back button moved from inside card to fixed top-left of screen with safe-area-inset for iPhone
- CropModal added: canvas-based crop/zoom after image selection, drag + pinch + scroll wheel, confirm crops to 400×400 JPEG
- Profile.jsx: Edit Profile button navigates to `/profile-setup?mode=edit`

### PostCard Action Row
- Vote pill replaced with 3 equal individual pills: Like, Dislike, Comment
- Each pill: icon + count, same size/style, flexShrink:0
- Spacer div between comment pill and save/flag buttons
- Flag always visible, never pushed off screen

### Create Page Fix
- Fixed black screen crash — `fieldErrors`, `setFieldErrors` state was declared but `employerRef`, `ratingRef`, `headerRef` refs were referenced in JSX but never declared
- All three refs now declared, page works normally

### Messaging (DM)
- Enter key = new line (textarea, not input)
- Date label shows once per message cluster (not under every bubble)
- fetchMessages dependency loop fixed (otherUser removed from useCallback deps)

### SideDrawer
- Dark/light mode toggle button margin removed — now aligns with other nav items

### FlagModal (mobile task 2)
- To/Subject header hidden on mobile via isMobile check (later removed entirely)

### 6-task subagent batch (April 12 evening)
1. Action row spacing fixed
2. Report popup To/Subject hidden mobile
3. Top Rated: star-only companies show with "No emoji reviews yet"
4. Post submit validation: always clickable, red field highlights, auto-scroll
5. DM Enter = new line (textarea)
6. Date label once per cluster

## NDA — Clocked App (April 13)
- Lukas and Tyler Dickinson are the Information Providers
- Michael Brown is the Recipient
- Full NDA drafted and saved to: `/Users/kylebutler/.openclaw/workspace/NDA-Clocked-2026.md`
- Based on LawDepot template, Michigan law, 2-year term
- Signed date: April 19, 2026
- Michael Brown's address is still blank — needs to be filled in before signing
- No witness section (Lukas requested removal)
- Clean version ready to paste into Google Docs

## Telegram — Clocked Reports Group
- Group: "Clocked Reports"
- Chat ID: -5000785765
- Bot: @Clockedreportsbot
- Lukas is admin, bot is admin
- All report notifications go here, not to main OpenClaw chat
- Delete button on notifications works via GET admin-delete route
