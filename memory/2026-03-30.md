# Session Notes — 2026-03-30

## Clocked App — Major Build Day (v1 → v39)

### Bug Fixes
- Auth flash on reload — Profile page + topbar now show neutral state while auth loads
- Email removed from profile display name fallback (was showing email prefix as username)
- Email moved out of profile page, now shows subtly near Sign Out in ••• menu
- Railway CDN cache busting — must bump vite version + run `railway redeploy --service frontend --yes`
- theclocked.com confirmed live and pointing to Railway frontend

### Post Creation
- Headline now mandatory, body optional
- Photo-only posts allowed (with headline)
- Photo-only comments allowed
- Backend updated to match all new validation rules

### Report/Flag Modal
- Added "I don't like it" at top, "Explicit material" above Other
- "Other" option now shows a text box for custom reason
- Success screen: animated green checkmark, "Your report has been submitted" message, gray X top right
- Flag modal now renders via React portal — closing it no longer exits comment section
- Flag modal z-index raised above comment sheet

### Comment System — Full Rebuild (Instagram-style)
- Comment button → bottom sheet slides up (no page navigation)
- Full PostCard shown at top of sheet (pinned, never pushed by comments)
- Comments newest-first (backend + frontend)
- New comments appear at top instantly (optimistic UI)
- Image-only comments supported
- Heart icon on comments — likes, persists, red fill
- Reply threading — nested under parent comment
- Replies trigger notifications in Alerts tab
- Comment char limit: 1600 including spaces
- Edit/Delete/Cancel modal (centered, no browser confirm)
- Drag handle to close sheet (swipe down)
- Drag-to-close also works from post area when scrolled to top
- Background fully locked when sheet open (iOS position:fixed fix)
- Bottom nav + sidebars + topbar hidden while sheet open, restored on close
- Sheet opens to full height instantly, comments preload before animation
- Desktop: sheet constrained to 740px feed width
- Comment button disabled/grayed while sheet already open
- No focus ring / highlight bubble after tapping comment button

### Post Cards
- Fully non-interactive (no tap highlight, no hover, no press state)
- Click-to-post-page navigation removed entirely
- Profile tap only on avatar + name (not full row)
- ••• dots: bold, small, tight
- Post ••• now opens centered modal (Edit/Delete/Cancel) instead of dropdown
- Comment count stays inline on mobile (no wrapping)
- Small gaps between cards (4px margin-bottom + border separator)
- Feed background matches card color (no black gaps in dark mode)

### Performance
- Profile tab data cached — instant switching after first load
- Feed no longer clears posts before new ones arrive (no reorder flash)
- Star ratings update instantly (optimistic)
- Global transitions reduced 0.15s → 0.08s
- Google Places API upgraded to v1 (fresher data)

### Deploy Process (locked in)
- Frontend: `rm -rf dist && railway up --service frontend && railway redeploy --service frontend --yes`
- Always verify: `curl -s https://www.theclocked.com | grep -o 'assets/index-[^"]*js'`
- Bump vite version on each deploy to bust Railway CDN cache
- Backend: git push to main (Railway auto-deploys)
- Current frontend version: v39

### Token Cost Policy (enforced)
- Small UI fixes: `--model claude-haiku-3-5`
- Medium tasks: Gemini Flash
- Complex/risky: Sonnet only when quality requires it
