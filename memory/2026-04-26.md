# Memory — 2026-04-26

## Clocked — Poll Feature Fixes (April 26, ~12:45 PM)

### Root cause
The `polls`, `poll_options`, and `poll_votes` tables had a migration file written but were **never applied to the Railway production database**. Every poll creation was silently failing — the post saved fine but no poll was stored.

### Fixes deployed
1. **DB tables created** — Ran migration manually against Railway production DB to create `polls`, `poll_options`, `poll_votes`
2. **Max poll options 4 → 10** — Updated in `Create.jsx` (frontend) and `posts.js` (backend validation)
3. **Polls tab filter fixed** — Prisma filter changed from `{ is: {} }` (no-op, returned ALL posts) to `{ isNot: null }` (only posts with polls) in backend `posts.js`
4. **Backend redeployed** — Pushed to GitHub to trigger Railway auto-deploy

### Key lesson
When polls weren't showing up, the real problem was missing DB tables, not frontend logic. Always verify production DB schema when a feature completely fails to persist.

### Files changed
- `clocked/app/frontend/src/pages/Create.jsx` — max options 4→10
- `clocked/app/backend/src/routes/posts.js` — filter fix + validation update
