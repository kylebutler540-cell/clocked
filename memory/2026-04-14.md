# Memory — April 14, 2026

## GitHub Token
- Token stored in git credential helper (no expiration)
- User: kylebutler540-cell
- Remote set on: `/Users/kylebutler/.openclaw/workspace/clocked/app`

## Clocked App — Changes Today (April 14)

### DM Width Fix (desktop)
- ConversationView uses `useLayoutEffect` to measure actual viewport and center in it
- Sidebar is hidden when fullscreen, so panel centers in full viewport width (max 680px)
- CSS class `.dm-conversation-overlay` in globals.css for base styles

### Admin Account — kylebutler540@gmail.com
- Can delete any post (already existed via isAdmin check)
- Can edit any post (added today)
- Can delete any comment (added today)
- Can edit any comment (added today)
- Messages bypass request flow — auto-accepted, go through immediately
- Frontend: PostCard, CommentSheet show delete/edit buttons for admin
- Backend: posts.js, comments.js, messages.js all have admin bypass

### Soft-Reject Message Requests
- Backend reject route sets `status: 'rejected'` (not deleted)
- Inbox filters out rejected convos (`status: { not: 'rejected' }`)
- Opening chat from profile after rejection still shows Accept button
- Sender can still re-message, receiver can still accept later

### Message Notifications → Messages Tab Only
- Backend: `message_request` and `message_accepted` excluded from /notifications and unread-count
- New endpoint: `GET /messages/unread-count` — returns unread messages + pending requests
- NotifContext: added `unreadMessages` state, polls `/messages/unread-count` every 15s
- BottomNav + LeftSidebar: Messages tab shows `unreadMessages` badge
- Opening Messages page clears the badge

### Profile Layout (public profile)
- Avatar left, name + handle + follower/following counts stacked to the right
- Follow + Message buttons full-width below everything, wider/shorter (36px tall, flex:1)

### Follow System Fixes
- Singular/plural: "1 Follower" not "1 Followers", everywhere including DM view
- FollowListModal: loads follow states before rendering list (no flash of wrong button)
- Optimistic follow/unfollow with server confirmation
- Profile cache invalidated after follow so counts stay fresh
- following_count fixed in DB (was stuck at 2, reset to 0)

### Performance — Stale-While-Revalidate Cache
- cache.js: added `lsGet`, `lsSet`, `lsIsFresh`, `lsDelete`, `lsClear` (localStorage layer)
- Profiles: cached to localStorage, load instantly on revisit
- Follower/following lists: show cached immediately, fetch fresh in background
- Inbox: shows cached conversations instantly on reopen
- DM threads: show previous messages instantly when reopened

### Other Fixes
- Removed duplicate "Message request sent" message from middle of chat (kept bottom bar only)
- Mobile scroll snap-back fixed: `overscroll-behavior-y: none` on `.page-content`
- ScrollToTop only fires on actual route changes, not re-renders

## Backup Status
- All code on GitHub: kylebutler540-cell/clocked (up to date)
- Backend + Frontend on Railway (auto-deploys)
- Database on Railway PostgreSQL (managed)
- Mac mini is NOT a single point of failure — everything recoverable from any computer
