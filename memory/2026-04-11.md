
## PERMANENT RULE — DO NOT CHANGE THE BACKEND RATE LIMITING OR AUTH FLOW

As of April 11, 2026 at ~11:04 AM, the backend is working correctly:
- Rate limit: 1000 requests per 15 min (was 100 — that's what was breaking everything)
- Auth endpoints are excluded from rate limiting entirely
- Google sign-in works
- The session/token flow is stable

**LUKAS'S STANDING ORDER:** Never touch the backend rate limiter, auth routes, or token/session logic again unless Lukas explicitly says so. If a task involves Saved, Create, Feed, Messaging UI, Profile, or anything frontend — only change that specific thing. The backend auth/rate-limit config must remain exactly as it is.

This rule overrides any future task that might accidentally affect:
- /api/auth/* routes
- Rate limiter settings in index.js
- JWT token generation or validation
- Session/anonymous user logic
- AuthContext.jsx token restoration logic
