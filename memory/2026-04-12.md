# Memory — April 12, 2026

## TOKEN CONSERVATION — STANDING ORDERS
- Minimum replies, no narration while working
- Do task, say "done" when finished
- /new between sessions to reset context
- Never iterate 10x on same problem — solve it once right

## Clocked Logo Status (as of April 12 AM)
- Logo: `/logo-text.jpg` (dark mode, black bg + purple, mix-blend-mode:screen)
- Logo: `/logo-text-light.png` (light mode, white bg + purple SVG render)
- Component: `ClockedLogo.jsx` uses `useTheme()` to swap between them
- Top bar: height=36, Sidebar: height=52, Mobile header: height=42
- Sidebar logo centered with `margin: 0 auto`
- All deployed as of ~9:41 AM

## Clocked App — Current State (April 12)
- Backend: rate limit 1000/15min, auth endpoints excluded — DO NOT TOUCH
- Frontend: Express server with no-cache headers on index.html
- All messaging, auth, profile setup working
- Google sign-in: custom button overlaying real GSI button
- Profile setup: fullscreen portal overlay, blocks navigation until complete
- Morning cron: simple "turn your ringer on / say good morning to girlfriend" at 7 AM
# Memory — April 12, 2026

## TOKEN CONSERVATION — STANDING ORDERS
- Minimum replies, no narration while working
- Do task, say "done" when finished
- /new between sessions to reset context
- Never iterate 10x on same problem — solve it once right

## Clocked Logo Status (as of April 12 AM)
- Logo: `/logo-text.jpg` (dark mode, black bg + purple, mix-blend-mode:screen)
- Logo: `/logo-text-light.png` (light mode, white bg + purple SVG render)
- Component: `ClockedLogo.jsx` uses `useTheme()` to swap between them
- Top bar: height=36, Sidebar: height=52, Mobile header: height=42
- Sidebar logo centered with `margin: 0 auto`
- All deployed as of ~9:41 AM

## Clocked App — Current State (April 12)
- Backend: rate limit 1000/15min, auth endpoints excluded — DO NOT TOUCH
- Frontend: Express server with no-cache headers on index.html
- All messaging, auth, profile setup working
- Google sign-in: custom button overlaying real GSI button
- Profile setup: fullscreen portal overlay, blocks navigation until complete
- Morning cron: simple "turn your ringer on / say good morning to girlfriend" at 7 AM

## Clocked — Updates (April 12 afternoon session)

### Logo
- New transparent PNG logo deployed — single file `/logo.png` for both light/dark
- `ClockedLogo.jsx` simplified to use one file, no more theme swapping
- `mix-blend-mode: screen` removed (was causing render flash on load)
- Sidebar logo removed (user requested)
- Top bar logo size tuned down, topbar height normalized
- Gray divider above Home button in sidebar removed

### Report System
- `backend/src/lib/mailer.js` — Resend-based email (was nodemailer/Gmail SMTP, Railway blocks SMTP)
- `backend/src/lib/telegramNotify.js` — Telegram notification on every report
- Resend API key set in Railway: `RESEND_API_KEY`
- Resend domain `theclocked.com` verified via DNS records added to GoDaddy
- Sends from `reports@theclocked.com` to `kylebutler540@gmail.com`
- Telegram env vars set: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID=8649191004`
- Both fire on every flag/report submission
- Admin delete button added to PostDetail.jsx — only visible to `kylebutler540@gmail.com` / `clockedreports@gmail.com`
- Delete bypasses ownership check for those emails on backend

### GoDaddy API credentials (used for DNS)
- Key: `hk8MRZwx7TVS_H2grwrTtEUxqRz17TD6Si2`
- Secret: `H4VX4XxdBesBpBWSem7kiq`

### Resend
- Account email: (throwaway, not Lukas's main)
- API key in Railway as `RESEND_API_KEY`

### UI Changes
- Top Rated icon changed to rocket ship (sidebar + drawer)
- Filter system added to Top Rated: Highest Rated / Lowest Rated / Nearest to Me
- Filter button uses equalizer/sliders icon
- Full-screen overlay closes dropdown on tap-outside (mobile fix)
- History tab removed from sidebar and drawer
- ScrollToTop component added — every page starts at top on navigation
- Mobile search bar: logo smaller, bar bigger, shifted left
- Side drawer width set to 50vw (max 280px), dividers removed
- `overscroll-behavior-x: none` added to kill horizontal page swipe on mobile

### Distance Feature
- `employer_lat` and `employer_lng` columns added to Post table (Prisma schema + db push)
- Posts geocoded on creation (non-blocking, Google Maps API)
- All 17 existing posts backfilled with coords
- Feed endpoint accepts `userLat`/`userLng` query params, returns `distance_miles` per post
- PostCard shows distance next to address
- Feed.jsx waits for geolocation before first fetch (coordsReady flag)
- Address truncation removed (was max-width: 140px on .post-meta-time)
