# Memory — 2026-03-27

## EOD Summary — Session Wrapped

### Final Fixes (Evening)
- **Google Sign-In fixed**: `VITE_API_URL` env var in Railway was missing `/api/` — was hitting wrong endpoint. Fixed in `api.js` hardcoded fallback AND updated Railway env var to `https://backend-production-7798f.up.railway.app/api/`
- **Vite build cache busting**: bumped output filenames from `-v2` to `-v3` to force fresh builds
- **Google OAuth origins**: Added `https://www.theclocked.com` and `https://theclocked.com` to Google Cloud Console authorized JS origins + redirect URIs
- **Location search**: Fixed LocationModal to use `api.js` (axios instance) instead of raw fetch — was breaking because CORS blocked direct Google Places calls from browser
- **Location search working**: Backend routes `/api/locations/search` and `/api/locations/reverse` confirmed working
- **Distance on desktop**: Added `/api/locations/reverse-city` endpoint + updated EmployerSearch to geocode city name if GPS coords not available

### Key Lesson
- `railway up` CLI deploys DON'T pick up Railway env vars during Vite build — must hardcode fallback in source OR set the var AND rebuild after
- Axios baseURL with path (e.g. `.../api`) requires trailing slash — otherwise relative paths replace instead of append

---

## Clocked App — Major Work Today

### DNS / Domain (theclocked.com)
- GoDaddy DNS updated: `www` CNAME → `tnj7l6bv.up.railway.app` (Railway's actual target for this domain)
- Added TXT verification record: `_railway-verify.www` → `railway-verify=121451b6c0c6db5cb6ea4b7fb9fa183e80048ef4baf2201ed066e82f36e287ea`
- Domain now loading (cert pending, Railway verifying TXT)
- Signed into GoDaddy via Google (kylebutler540@gmail.com) using OpenClaw browser on Safari

### Frontend Changes Deployed
- Onboarding screens: white background, larger sections, removed gray blocks/side lines, bigger text
- Removed "Browse First" button from welcome screen — only "Get Started" now
- CSS: dark mode default bug fixed (`:root` now defaults to light mode, `[data-theme="dark"]` added)
- Inline script in `index.html` applies theme before React loads (no more black flash on first load)

### Backend Changes Deployed
- New route: `GET /api/locations/search?query=` — city autocomplete via Google Places
- New route: `GET /api/locations/reverse?lat=&lng=` — reverse geocode coords to city
- New route: `GET /api/locations/reverse-city?city=` — geocode city name to lat/lng (for desktop distance)
- LocationModal now calls backend instead of Google Places directly (was failing due to CORS)
- EmployerSearch: distance now shows on desktop too — geocodes saved city name to coords if GPS coords not available

### Database Cleanup
- Deleted 3 junk posts: cmn3vroz3002b8v2dmf5v245b, cmn3tg1uv001y8v2dq8xp4mfg, cmn3sx6ns001r8v2dc0ny4ia1
- Remaining posts: "Love my manager" (Walmart) and "Good first job" (Walmart)
- NOTE: "Management is an absolute joke" Walmart post and Amazon delivery station post were NOT in DB — need to be recreated

### Trading Bot
- Bot stopped running sometime after 8 AM — restarted at ~3:42 PM
- Active BTC position: 0.001084 BTC bought @ $66,414.49
- Portfolio: ~$88 (started $100.49) — fees dragging performance
- Bot running v3 strategy (RSI <28 buy, >68 or +3.5% sell, -3% stop-loss)
- Need to keep an eye on why bot stopped — may need a cron/launchd watchdog

### Chrome Browser Issue
- OpenClaw browser tool requires Chrome launched with `--remote-debugging-port=9222`
- Chrome needs to be killed first (`pkill -f "Google Chrome"`) then relaunched with the flag
- macOS Accessibility permissions needed for Terminal and node to control apps
- Lukas enabled both in System Preferences during session

### SanDisk Extreme SSD
- Lukas has a SanDisk Extreme 2TB plugged into Mac mini
- Shows as `/Volumes/Extreme SSD` — 1.8TB available, 23GB used
- Works fine, plug-and-play

## Domain Renewal Reminder
- GoDaddy cron job set: `godaddy-domain-renewal` fires March 10, 2027
- Domain: theclocked.com, account: kylebutler540@gmail.com

## Deploy Notes
- Frontend: always deploy via `cd clocked/app/frontend && railway up --service frontend`
- Backend: `git push origin main` from `clocked/app/backend` triggers Railway auto-deploy — BUT sometimes Railway doesn't pick it up, use `railway up --service backend` to force it
- GitHub remote is at: `clocked/app/backend` (not workspace root)
