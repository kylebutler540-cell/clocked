# 2026-03-26

## App Work (Clocked)

### UI Changes deployed
1. **Saved page** — added `.saved-page` class, expands to full 740px feed width on desktop (matches homepage layout)
2. **Back button** — moved to left edge of `app-main`, sits just right of the sidebar instead of floating above content
3. **iOS sticky search bar** — fixed with `position: fixed` on `.mobile-only-header` (same approach as bottom nav). Adds `padding-top: var(--header-height)` to `.page-content` to compensate.

### Domain setup (theclocked.com → Railway)
- Registered `www.theclocked.com` as custom domain on Railway (service: frontend)
- Railway CNAME target: `tnj7l6bv.up.railway.app`
- Updated GoDaddy `www` CNAME → `tnj7l6bv.up.railway.app` ✅
- Set up GoDaddy forwarding: `theclocked.com` → `https://www.theclocked.com` (301) ✅
- SSL cert provisioning in progress (Railway auto-provisions, takes up to 1hr)
- Need to check ~8PM ET if www.theclocked.com is live

### Pending
- TXT verification record (`_railway-verify`) for Railway — failed due to GoDaddy OTP issues, not critical for site to go live
- Railway also has old `theclocked.com` domain registered (without www) — not active since GoDaddy won't allow root CNAME
