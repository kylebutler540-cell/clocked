# Memory — 2026-04-21

## PostHog Installed on Clocked ✅
- PostHog analytics wired into Clocked frontend (React/Vite)
- Key stored as `VITE_PUBLIC_POSTHOG_KEY` in Railway env vars
- Tracks page views, clicks, session replays automatically
- Lukas signed up for PostHog premium (pay-as-you-go)
- Project: "Default project" — no need to rename

## Critical Bug Fixed: PostCard.jsx JSX Comment
- Found and fixed a pre-existing bug: `{/* Actions row */` was missing closing `}`
- This was silently breaking ALL builds — Railway was serving stale dist/ for a long time
- Fixed to `{/* Actions row */}` — builds now work properly
- New deployed hash: `assets/index-BVv8Z4B1.js`

## Build System Discovery
- Railway frontend was NEVER successfully rebuilding — builds failed silently
- `railway up` uploads but Railway runs `npm run build` which was failing on PostCard.jsx
- Server.js serves from dist/ — Railway falls back to last successful dist on build failure
- Now fixed: builds succeed, deploys work

## AI App Dev Research — Key Takeaways for Clocked
From deep research across 25+ sources:
1. Create `CLAUDE.md` at project root — keeps AI agents on-pattern across sessions
2. Plan-first workflow — never one-shot complex features
3. Feature-based folder structure: `src/features/[feature]/`
4. Add DB indexes on `employer_id`, `created_at`, `user_id`
5. React Query / TanStack Query for all data fetching
6. Skeleton loaders over spinners everywhere
7. Virtualize long feed lists with `@tanstack/react-virtual`
8. Use `ultrathink` keyword in Claude Code for hard problems
9. Revert and restart after 2-3 failed attempts — don't keep prompting broken code
10. Vertical slices (full feature end-to-end) not horizontal layers

## Session Context
- Lukas focused on improving Clocked development workflow today
- Discussed OpenClaw vs Claude Code — conclusion: keep OpenClaw as hub, use Claude Code as sub-agent
- PostHog onboarding was confusing — kept showing setup screens, needed "I'll pick myself" to skip
- Lukas frustrated by long debug sessions and excessive update messages — keep it concise
